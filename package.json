{
  "name": "nixie-parallel-class-sheet",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name=nixie-live-sheet"
  },
  "devDependencies": {
    "wrangler": "4.114.0"
  }
}
