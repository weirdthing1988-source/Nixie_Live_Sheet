{
  "name": "nixie-live-sheet",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name=nixie-live-sheet"
  },
  "devDependencies": {
    "wrangler": "4.114.0"
  }
}
